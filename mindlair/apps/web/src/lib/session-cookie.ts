/**
 * __Host- prefix: browser requires Secure, Path=/, and no Domain attribute.
 * See setSessionCookie in auth.ts — COOKIE_DOMAIN is ignored for this name.
 */
export const SESSION_COOKIE_NAME = '__Host-mindlair_session';
